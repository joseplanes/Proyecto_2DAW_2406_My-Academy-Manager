import { PatronClasePipe } from './patron-clase.pipe';

describe('PatronClasePipe', () => {
  it('create an instance', () => {
    const pipe = new PatronClasePipe();
    expect(pipe).toBeTruthy();
  });
});

import { PatronAlumnoPipe } from './patron-alumno.pipe';

describe('PatronAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new PatronAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
